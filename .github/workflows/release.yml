name: Build and Publish Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Check out tagged source
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build wheel
        run: |
          python -m pip install --upgrade pip build
          python -m build
          ls -lh dist/

      - name: Verify wheel package
        run: |
          python -m venv /tmp/sirgon-release-test
          /tmp/sirgon-release-test/bin/python -m pip install --upgrade pip
          /tmp/sirgon-release-test/bin/python -m pip install dist/*.whl
          /tmp/sirgon-release-test/bin/diskqual --version
          /tmp/sirgon-release-test/bin/python -c "import diskqual, diskqual.tui, diskqual.engine, diskqual.reporting, diskqual.labels; print('Package imports OK')"

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          TAG="$GITHUB_REF_NAME"
          FLAGS=()
          case "$TAG" in
            *-alpha*|*-beta*|*-rc*) FLAGS+=(--prerelease) ;;
          esac

          gh release create "$TAG" \
            dist/*.whl \
            --title "Sirgon DiskQual ${TAG#v}" \
            --generate-notes \
            "${FLAGS[@]}"
